{
  "name": "template-ui",
  "version": "1.0.0",
  "private": true,
  "description": "TemplatePlugin web UI, bundled for embedding into the plugin binary",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node dev-server.mjs"
  },
  "dependencies": {
    "lit": "^3.3.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
